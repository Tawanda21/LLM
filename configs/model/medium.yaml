# Medium model — ~350 M parameters (GPT-2 medium scale), multi-GPU recommended
vocab_size: 32000
dim: 1024
n_layers: 24
n_heads: 16
n_kv_heads: 8 # GQA: 2 Q heads per KV head
max_seq_len: 2048
multiple_of: 256
dropout: 0.0
norm_eps: 1.0e-6
rope_theta: 10000.0
